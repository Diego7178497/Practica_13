<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Ejercicio6.MainPage">

    <ContentPage.Resources>
        
    </ContentPage.Resources>

    <StackLayout BackgroundColor="DarkSlateBlue">
        <StackLayout.Resources>
            <ResourceDictionary>
                <Style x:Key="MyEntryStyle" TargetType= "Entry">
                    <Style.Triggers>
                        <Trigger TargetType="Entry" Property="IsFocused"
                     Value="True">
                            <Setter Property="TextColor" Value="Red" />
                            <Setter Property="FontSize" Value="30" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ResourceDictionary>
        </StackLayout.Resources>

        <Entry Style="{StaticResource MyEntryStyle}" Text="Libro: Aplicciones de"/>
        <Entry Style="{StaticResource MyEntryStyle}" Text="Negocio con"/>
        <Entry Style="{StaticResource MyEntryStyle}" Text="Xamatin.Froms"/>

    </StackLayout>

   


</ContentPage>
